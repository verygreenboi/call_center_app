<Gather numDigits="1" action="<%= calls_routing_url %>.xml?step=routing" method="GET">
	<Say voice="woman">
		<% unless @bank.nil? %>
			<%= @bank.greeting %>
		<% else%>
			An error occurred.
		<% end %>
	</Say>
</Gather>